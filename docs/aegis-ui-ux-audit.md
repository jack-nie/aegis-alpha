# Aegis Alpha UI/UX 交互审计与工程交�?
审计时间�?026-05-05  
审计对象：本�?`http://127.0.0.1:5174/` 前端�?`http://127.0.0.1:5178/_backend` 后端  
登录账号：`guanghui.nie / guanghui.nie`  
审计方式：使用真�?Chrome + Playwright 控制页面，逐页导航、点击、表单输入、刷新、直�?URL、桌�?平板/移动视口验证；未使用截图作为判断依据�?
原始验证资产�?
- 自动化脚本：`.ai-memory/ui-audit-tools/audit-aegis-ui.mjs`
- 原始结果：`.ai-memory/ui-audit-tools/aegis-ui-audit-raw.json`
- 主要代码入口：`aegis-alpha-web/app/App.jsx`、`aegis-alpha-web/app/globals.css`

## 结论摘要

系统已具�?Alpha 级可演示骨架：登录、主导航、组合、Agent、工作流、运行中心、市场数据、审计日志可以跑通。但当前交互仍偏“单页原型”，离可交付的业务系统有明显差距。最需要优先解决的是：真实路由、响应式布局、弹层可访问性、组合数据一致性、运行中心可操作性、语言切换和占位页面治理�?
## P0：必须先解决

### 1. 路由是前端内存状态，刷新和直达链接断�?
证据�?
- 点击“持仓”后浏览�?URL 仍为 `http://127.0.0.1:5174/`�?- 在持仓页刷新后，页面回到首页�?- 直达 `http://127.0.0.1:5174/portfolio/assets` 显示“页面不存在 请从左侧导航重新进入 Aegis Alpha Platform。�?- 代码位置：`App.jsx` �?`const [path, setPath] = useState("/")` �?`const navigate = (target) => setPath(target)`；页面分发集中在 `if (path === ...)`�?
影响�?
- 用户无法分享链接、刷新恢复、浏览器前进后退、书签保存�?- 外部系统、通知、审计日志、运行结果不能跳转到具体页面�?
前端整改�?
- 使用 Next App Router 或至少同�?`history.pushState` / `popstate`，让 URL 成为导航状态来源�?- 为核心页面建立真实路由：`/portfolio`、`/portfolio/assets`、`/portfolio/portfolios`、`/agent`、`/workflow`、`/workflow/runs`、`/data-center/dashboard`、`/governance/audit`�?- active nav、Header 标题、刷新恢复都基于真实 pathname�?
后端/平台整改�?
- 若部署层使用 Next standalone，需要确保未知业务路径回源到前端路由，而不是落�?not-found�?- 后端返回�?runId、traceId、portfolioId 等可跳转实体应提供目�?URL 字段或前端约定�?
验收标准�?
- 直达 `/portfolio/assets` 能进入持仓页�?- 在任一二级页刷新后仍停留当前页�?- 浏览器前�?后退可恢复上一页�?
### 2. 移动端和平板布局不可�?
证据�?
- 390px 移动视口�?Header 语言选择框出现在 x=535，已经在视口外�?- 390px 移动视口下仍保留固定 `255px` 侧边栏，主内容被挤压�?- 代码位置：Sidebar 使用 `w-[255px]`；Header 使用固定 `h-16 flex justify-between px-6`；App 根布局为固定横�?`flex`�?
影响�?
- 移动端看不到关键 header 操作；内容和导航互相挤压�?- 平板窄屏下操作按钮、表格、侧栏都需要横向滚动或被遮挡�?
前端整改�?
- `lg` 以下�?Sidebar 改为抽屉式导航，默认收起，Header 显示菜单按钮�?- Header 操作区分组：用户信息折叠到菜单，语言切换和退出放入用户菜单�?- 为表格页面提供移动卡片视图或关键列优先显示�?- 为工作流画布、运行中心、组合列表定义窄屏降级布局�?
验收标准�?
- 390px�?68px�?440px 三档视口�?header 控件跑出视口�?- 移动端可以完成登录、进入持仓、打开 Agent 弹窗、查看运行中心详情�?
### 3. 弹窗/抽屉没有 dialog 语义与焦点管�?
证据�?
- 打开“新�?Agent”弹窗后，页面中 `role="dialog"` �?`aria-modal="true"` 数量均为 0�?- 在弹窗打开状态按 Tab 两次，焦点移动到背景 Agent 卡片的“启动”按钮�?- 打开“新建交易”抽屉后同样没有 dialog 语义，背景和抽屉内容同时出现在主文本流�?- 代码位置：Agent 弹窗 `fixed inset-0`；交易抽�?`fixed bottom-0 right-0 top-16`�?
影响�?
- 键盘用户和读屏用户无法正确感知弹窗�?- 焦点逃逸会导致误操作背景按钮�?- Esc 关闭、初始焦点、关闭后焦点恢复都不可控�?
前端整改�?
- 建立统一 `Modal` / `Drawer` 组件，默认实�?`role="dialog"`、`aria-modal="true"`、标题关联、焦点陷阱、Esc 关闭、背�?inert�?- 弹窗打开时焦点进入第一个可操作控件，关闭后恢复到触发按钮�?- 交易抽屉要明确标题、关闭按钮、保存状态和未保存确认�?
验收标准�?
- 弹窗打开�?Tab 只能在弹窗内循环�?- 读屏可读出弹窗标题�?- Esc 关闭，关闭后焦点回到触发按钮�?
## P1：高优先级体验问�?
### 4. 组合模块的信息架构和数据一致性混�?
证据�?
- `/portfolio/assets` �?Header 标题仍显示“投资组�?/ 总览”，代码�?`pageTitles["/portfolio/assets"] = "投资组合 / 总览"`�?- “总览”实际进入交易录入页，用户预期的组合概览不存在�?- 组合列表显示 `Trading Portfolio 2` �?6 个资产�?5 笔交易，�?`/portfolio/trades` 当前返回 `[]`，持仓页显示“该组合暂无资产”�?- 组合列表可用，但导航文案“组合”容易与“投资组合”父菜单、页面里的“组合模式”产生命中和理解歧义�?
影响�?
- 用户不知道“总览/持仓/组合/交易”的关系�?- 资产和交易计数与明细不一致，削弱系统可信度�?- 空状态不可信：它可能是数据缺失、筛选为空，也可能是后端未接入�?
前端整改�?
- 重构组合导航：`组合概览`、`持仓`、`交易流水`、`组合列表`�?- 修正 Header 标题和面包屑�?- 持仓页显示当前选择组合、数据更新时间、空状态原因�?- 新建交易入口进入“交易流水”或抽屉，但不要覆盖持仓页主任务�?
后端整改�?
- 对齐组合摘要、持仓明细、交易流水的数据源�?- 提供明确端点：`GET /portfolio/{id}/positions`、`GET /portfolio/{id}/trades`、`GET /portfolio/{id}/summary`�?- 如果当前只有 seed 摘要，没有明细，接口应返�?`dataCompleteness` �?`sourceStatus`，前端据此展示“摘要已导入，明细待同步”�?
验收标准�?
- 组合列表资产�?交易数能下钻到对应明细�?- 持仓空状态只在真实无持仓时出现�?
### 5. 语言切换是无效控�?
证据�?
- Header 语言 select 从“中文”切�?“EN�?后，页面文本没有变化�?- 代码位置：Header �?`<select>` 没有 `value`、`onChange`、i18n 状态或持久化�?
影响�?
- 用户会认为系统支持中英文，但实际不支持�?- 这是典型“假控件”，会降低信任�?
前端整改�?
- 短期：如果没�?i18n，就移除语言切换�?- 中期：接�?i18n 字典，切换后立即更新 Header、导航、按钮、空状态、表格列名�?
后端整改�?
- 对后端错误消息、状态枚举提�?locale 映射，或统一返回错误 code，由前端翻译�?
验收标准�?
- 切换 EN 后导航、Header、主页面核心文案变为英文�?- 刷新后语言选择保持�?
### 6. 可访问性基础缺口较多

证据�?
- 首页搜索提交按钮为空文本，无 `aria-label`�?- 浮动 AI Copilot 按钮为空文本，仅�?`title`，没�?`aria-label`�?- Header 语言 select �?label�?- 登录页控制台提示密码输入缺少 `autocomplete="current-password"`�?- 运行中心存在大量小按钮；审计脚本统计该页 81 个高度或宽度低于 32px 的操作目标�?
影响�?
- 键盘、读屏、低视力用户使用困难�?- 自动化测试无法稳定定位意图�?
前端整改�?
- 所有图标按钮补�?`aria-label`，优先用 lucide 图标 + tooltip + 可访问名称�?- 登录输入补齐 `name`、`autoComplete`�?- Select、输入框都使用真�?label �?`aria-labelledby`�?- 行内按钮目标不小�?32px，主要操作不小于 40px�?
验收标准�?
- 自动化检查不再出现空名称按钮�?- Tab 顺序和视觉焦点清晰可见�?
### 7. 运行中心表格过宽，操作模型不清晰

证据�?
- 1440px 桌面下运行中心表格宽度约 1689px，操作列在远端，需要横向滚动�?- 每行同时显示 `trace / dispatch / pause / resume / cancel`，其中大量按钮按状态禁用，但仍占据视觉重量�?- 操作文案混用英文，和中文页面不一致�?
影响�?
- 用户很难快速判断“当前能做什么”�?- 高频运维页面扫描效率低，traceId/runId 长字段挤占业务字段�?
前端整改�?
- 固定关键列：状态、工作流、主题、操作；�?ID 收入详情抽屉或复制按钮�?- 每行只显示当前可执行�?1-2 个主操作，其他放入更多菜单�?- 状态文案本地化，禁用原因用 tooltip�?- 增加筛选：状态、工作流、时间范围、traceId 搜索�?
后端整改�?
- �?run 列表响应中返�?`availableActions`、`actionReasons`、`priority`、`lastEvent`�?- 支持分页、排序、筛选，避免前端一次渲染过多运行记录�?
验收标准�?
- 1440px 下无需横向滚动即可完成查看状态、打开详情、执行可用操作�?- 禁用操作有明确原因�?
### 8. 市场数据页面仍有原型痕迹和编码问�?
证据�?
- 宏观象限、信用脉冲仍是“占位”�?- 查询 MSFT 后可以返回行情、财报和新闻，但 Yahoo 新闻标题出现 `â` 乱码�?- 时间字段直接显示 UTC/ISO，例�?`2026-05-04T22:00:19Z`，未本地化到用户时区�?
影响�?
- 占位图降低数据中心可信度�?- 新闻标题乱码会直接影响专业感�?- 用户无法判断行情延迟和时间语义�?
前端整改�?
- 占位图要么隐藏，要么替换成真实空状�?接入状态�?- 行情卡显�?provider、延迟说明、更新时间本地化�?- 财报指标需要中文名、单位、期间、同�?环比，而不是原�?XBRL tag 列表�?
后端整改�?
- 修复 RSS/HTML 实体与字符编码处理，保证新闻标题�?UTF-8 正文�?- 市场数据响应增加 `asOfLocal` 或统一返回 UTC + timezone 元数据�?- 财报指标输出前做业务归一化：收入、净利润、经营利润、EPS、期间�?
验收标准�?
- MSFT 新闻标题不再出现乱码�?- 市场数据页面没有“占位”字样�?
## P2：中期改�?
### 9. 可点击占位页面应下线或标明接入状�?
证据�?
- “报告”“自选股”“回测管理”等导航项可进入，但主内容是“内容占位”�?
建议�?
- 若近期不交付，隐藏入口�?- 若保留入口，显示接入状态、目标能力、预计上线范围，不要让用户误以为功能可用�?
### 10. 工作流画布需要更强的任务导向

证据�?
- 工作流打开后节点目录非常长，画布控制按钮为空文本且尺寸 28px�?- 只读工作流仍展示节点目录和大量技�?handler，对业务用户负担较重�?
建议�?
- 增加“运行配�?/ 执行结果 / 节点详情”三段式任务流�?- 技�?handler 默认折叠，节点目录支持分类筛选和搜索结果计数�?- 画布控制按钮补可访问名称�?tooltip�?
## 高级前端工程师任务清�?
1. 重构路由：将 `path` 内存状态迁移为真实 URL 路由，完成刷新、直达、前进后退�?2. 响应式布局：实现移动端抽屉导航、Header 用户菜单、表格移动降级�?3. 建立统一 `Modal` / `Drawer` 组件：语义、焦点陷阱、Esc、inert、焦点恢复�?4. 组合模块 IA 调整：组合概览、持仓、交易流水、组合列表分层清晰�?5. 修复 Header 标题、面包屑、导�?active 规则和占位入口治理�?6. 修复可访问性：空名称按钮、select label、autocomplete、按钮尺寸、tooltip�?7. 重做运行中心列表：列优先级、可用操作、筛选、详情抽屉�?8. 市场数据页面去占位，展示可解释的 provider、延迟、更新时间和数据空状态�?
## 高级后端工程师任务清�?
1. 支持前端真实路由部署：确保业务路径不会被服务�?not-found 截断�?2. 对齐组合数据契约：summary、positions、trades 使用同一数据源或返回数据完整性状态�?3. 新增/完善组合详情 API：positions、trades、summary、数据更新时间、来源状态�?4. 运行中心 API 增加分页、筛选、排序、`availableActions`、禁用原因、最近事件摘要�?5. 市场数据修复新闻编码和时间格式元数据，归一化财报业务字段�?6. i18n 支持：错�?code、状态枚举、后端消息不要直接绑定单一语言文案�?7. 为可跳转实体返回稳定 URL 或前端可构造字段：run、trace、portfolio、recommendation�?
## 建议验收用例

1. 登录后进�?`/portfolio/assets`，刷新仍在持仓页�?2. 复制 `/workflow/runs?status=RUNNING` 到新标签可恢复筛选结果�?3. 390px 宽度下打开导航、进�?Agent、新�?Agent、关闭弹窗，所有控件可见�?4. 新建 Agent 弹窗打开后按 Tab 10 次，焦点不离开弹窗�?5. 组合列表中点击“查看持仓”，持仓页显示同一组合的真实持仓或明确数据未同步状态�?6. 查询 `MSFT`，新闻标题无乱码，更新时间按本地格式展示�?7. 运行中心只展示当前可执行操作，禁用操作有原因说明�?8. 语言切换�?EN 后，导航、Header、按钮、空状态同步变更；若未实现 i18n，则页面不出现语言切换控件�?